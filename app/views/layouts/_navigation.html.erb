<%= link_to "Zuminati", root_path, :class => 'brand' %>
<ul class="nav">
  <li>
    <%= link_to "Homes For Rent", listings_rentals_listings_path %>
  </li>
  <li>
    <%= link_to 'Add a listing', new_listing_path %>
  </li>
  <li>
    <form action="/listings" class='inline'>
      <input type="text" class="input-small search-query span3" name='q' value="<%= params[:q] %>" />
      <button type="submit" class="btn btn-small">Search</button>
    </form>
  </li>
    <% if user_signed_in? %>
    <li>
    <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', new_user_session_path %>  
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to 'Your Account', edit_user_registration_path %>
    </li>
    <% if current_user.has_role? :admin %>
      <li>
      <%= link_to 'Admin', users_path %>
      </li>
    <% end %>
  <% else %>
    <li>
    <%= link_to 'Sign up', new_user_registration_path %>
    </li>
  <% end %>
</ul>
